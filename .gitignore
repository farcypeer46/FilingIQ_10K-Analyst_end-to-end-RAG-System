# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# --- Virtual environment (do not commit; recreate from requirements.txt) ---
venv/
.venv/

# --- Secrets ---
.env
*.env

# --- OS junk ---
.DS_Store

# --- Generated / large artifacts (regenerable from the pipeline) ---
# Vector store: COMMITTED via git-lfs (see .gitattributes) so Hugging Face Spaces
# can build the serving image from the prebuilt index. Regenerable via
# ingestion/build_index.py, but shipped so deploys don't rebuild it.
# Downloaded filings + extracted corpus (fetched by ingestion/fetch_filings.py)
data/sec-edgar-filings/
data/extracted.json
# Eval outputs (regenerated by evaluation/evaluate.py)
evaluation/results.json

# --- Private engineering docs (the build "recipe" — kept OUT of the public repo
#     so the project can't be trivially replicated; a curated README is the public face) ---
CLAUDE.md
BACKLOG.md
LEARNINGS.md
